package com.soumya.lore.ui.theme

import androidx.compose.ui.graphics.Color

// Lore brand palette — calm, minimal, premium.
// Emerald is intentionally used ONLY for primary actions/active states,
// never as a background or large surface, so the app doesn't "feel green".
val LoreBackground = Color(0xFFFAFAF9)
val LoreSurface = Color(0xFFFFFFFF)
val LoreTextPrimary = Color(0xFF18181B)
val LoreTextSecondary = Color(0xFF71717A)
val LoreEmerald = Color(0xFF10B981)
val LoreOutline = Color(0xFFE4E4E7)
